% ===================================================================
% SEZIONE MOCKUPS
% ===================================================================

\section{Progettazione dell'Interfaccia Utente (Mockups)}

Questa sezione presenta la progettazione dell'interfaccia utente (UI) per l'applicazione \textbf{JavaBrew}, tramite una serie di mockup. Tali rappresentazioni non si limitano all'aspetto estetico, ma delineano l'architettura dell'interazione uomo-macchina (HCI), specificando i flussi operativi previsti per ciascun ruolo utente. L'obiettivo è fornire una visualizzazione chiara dell'esperienza utente (UX), evidenziando come il design sia stato concepito per supportare le funzionalità del sistema in modo intuitivo ed efficiente.

\subsection{Flusso di Accesso e Registrazione}

Il flusso di onboarding è articolato in una schermata di autenticazione (Figura \ref{fig:login}) e un modulo di registrazione (Figura \ref{fig:register}). Le interfacce sono caratterizzate da un design essenziale, che richiede unicamente le informazioni strettamente necessarie, facilitando un processo di iscrizione e accesso diretto e privo di complessità superflue.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/login.png}
        \caption{Schermata di login.}
        \label{fig:login}
    \end{subfigure}
    \hfill % Aggiunge spazio flessibile tra le immagini
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/Create_acoount.png}
        \caption{Schermata di registrazione.}
        \label{fig:register}
    \end{subfigure}
    \caption{Interfacce per il flusso di accesso e registrazione.}
    \label{fig:login_register}
\end{figure}

\subsection{Dashboard per Ruolo Utente}

Ogni attore del sistema è dotato di una dashboard personalizzata, progettata per ottimizzare l'efficienza operativa in base al ruolo. La dashboard dell'Amministratore (Figura \ref{fig:admin}) funge da pannello di controllo centralizzato, offrendo una visione sistemica. Le interfacce dedicate al Cliente e al Tecnico (Figura \ref{fig:customer_worker_dashboards}) sono ottimizzate per l'utilizzo su dispositivi mobili.

\begin{figure}[H]
    \centering
    \screenshot[width=0.5\textwidth]{assets/admin.png}
    \caption{Dashboard dell'Amministratore.}
    \label{fig:admin}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/customer.png}
        \caption{Dashboard del Cliente.}
        \label{fig:customer_dash}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/worker.png}
        \caption{Dashboard del Tecnico.}
        \label{fig:worker_dash}
    \end{subfigure}
    \caption{Dashboard per i ruoli Cliente e Tecnico.}
    \label{fig:customer_worker_dashboards}
\end{figure}

\subsection{Catalogo Prodotti e Acquisto}

La schermata di selezione dei prodotti (Figura \ref{fig:items}) implementa un catalogo digitale interattivo. Il design, orientato alla chiarezza visiva e alla semplicità, è studiato per guidare l'utente attraverso un processo di selezione e acquisizione dei prodotti efficace e sequenziale.

\begin{figure}[H]
    \centering
    \screenshot[height=9cm]{assets/items.png}
    \caption{Schermata di selezione dei prodotti.}
    \label{fig:items}
\end{figure}

\bigskip
In sintesi, i mockup presentati illustrano come la progettazione dell'interfaccia utente di \textbf{JavaBrew} sia stata definita in base ai requisiti funzionali e alle esigenze specifiche di ciascun attore. Il design delle interfacce si concentra sull'efficienza operativa e sulla fruibilità, aspetti fondamentali per l'aderenza del sistema ai requisiti utente.