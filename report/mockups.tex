% ===================================================================
% SEZIONE MOCKUPS
% ===================================================================

\section{Progettazione dell'Interfaccia Utente (Mockups)}

In questa sezione viene illustrata la progettazione dell'interfaccia utente (UI) per l'applicazione \textbf{JavaBrew}. I mockup presentati di seguito non si limitano a definire l'estetica del sistema, ma rappresentano l'architettura dell'interazione uomo-macchina (HCI), dettagliando i flussi operativi previsti per ciascun attore. L'obiettivo è fornire una chiara visualizzazione dell'esperienza utente (UX), dimostrando come il design supporti le funzionalità del sistema in modo intuitivo ed efficiente.

\subsection{Flusso di Accesso e Registrazione}

Il flusso di onboarding inizia con una schermata di login minimale (Figura \ref{fig:login}) e prosegue con un modulo di registrazione (Figura \ref{fig:register}) progettato per essere rapido e trasparente. Le interfacce sono pulite, richiedono solo le informazioni essenziali e guidano l'utente in modo chiaro.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/login.png}
        \caption{Schermata di login.}
        \label{fig:login}
    \end{subfigure}
    \hfill % Aggiunge spazio flessibile tra le immagini
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/Create_acoount.png}
        \caption{Schermata di registrazione.}
        \label{fig:register}
    \end{subfigure}
    \caption{Interfacce per il flusso di accesso e registrazione.}
    \label{fig:login_register}
\end{figure}

\subsection{Dashboard per Ruolo Utente}

Ogni attore del sistema dispone di una dashboard personalizzata. La dashboard dell'Amministratore (Figura \ref{fig:admin}) è un centro di controllo strategico con una visione d'insieme. Le interfacce per il Cliente e il Tecnico (Figura \ref{fig:customer_worker_dashboards}) sono ottimizzate per l'uso mobile.

\begin{figure}[H]
    \centering
    \screenshot[width=0.5\textwidth]{assets/admin.png}
    \caption{Dashboard dell'Amministratore.}
    \label{fig:admin}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/customer.png}
        \caption{Dashboard del Cliente.}
        \label{fig:customer_dash}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \screenshot[height=9cm]{assets/worker.png}
        \caption{Dashboard del Tecnico.}
        \label{fig:worker_dash}
    \end{subfigure}
    \caption{Dashboard per i ruoli Cliente e Tecnico.}
    \label{fig:customer_worker_dashboards}
\end{figure}

\subsection{Catalogo Prodotti e Acquisto}

La schermata di selezione dei prodotti (Figura \ref{fig:items}) funge da catalogo digitale interattivo. Il design pulito e visuale guida l'utente attraverso un processo di selezione e acquisto fluido.

\begin{figure}[H]
    \centering
    \screenshot[height=9cm]{assets/items.png}
    \caption{Schermata di selezione dei prodotti.}
    \label{fig:items}
\end{figure}

\bigskip
In sintesi, i mockup presentati dimostrano come la progettazione dell'interfaccia di \textbf{JavaBrew} sia stata guidata dai requisiti funzionali e dalle necessità specifiche di ogni attore.
Queste interfacce non solo sono esteticamente gradevoli, ma sono anche funzionali e intuitive, garantendo un'esperienza utente coerente e soddisfacente. La progettazione si concentra sull'efficienza operativa e sulla facilità d'uso, elementi chiave per il successo dell'applicazione.